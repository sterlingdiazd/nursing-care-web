# Configuration for mobile development (Expo)
# Use your local network IP instead of localhost
# This allows mobile devices on the same network to access the API
VITE_API_BASE_URL=http://10.0.0.34:8080/api
